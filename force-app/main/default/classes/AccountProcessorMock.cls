@isTets
public with sharing class AccountProcessorMock implements HttpCalloutMock {
    public HttpResnse respond (HttpRequest req) {
        HttpResponse res = new HttpResponse();
        res.set
    }
}
